apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-suggestion
  namespace: web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms_suggestion
  template:
    metadata:
      labels:
        app: ms_suggestion
    spec:
      containers:
      - name: ms_suggestion
        image: ms_suggestion:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5000